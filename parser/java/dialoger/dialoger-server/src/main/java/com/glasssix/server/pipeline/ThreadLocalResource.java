package com.glasssix.server.pipeline;

public class ThreadLocalResource {
    public static ThreadLocal<String> longinusInstance = new ThreadLocal<>();
    public static ThreadLocal<String> gaiusInstance = new ThreadLocal<>();
    public static ThreadLocal<String> cassiusInstance = new ThreadLocal<>();
    public static ThreadLocal<String> irisvielInstance = new ThreadLocal<>();
}
